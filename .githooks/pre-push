#!/usr/bin/env bash

set -euo pipefail

while read -r _ _ remote_ref _; do
	case "$remote_ref" in
	refs/tags/v*)
		echo "ERROR: release tags are created by GitHub Actions." >&2
		echo "Run: go run ../automation/cmd/hq release lib sol --version <tag> --root .." >&2
		exit 1
		;;
	esac
done
